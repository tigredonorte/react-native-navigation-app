import { StyleSheet } from 'react-native';

export const CategoryStyles = StyleSheet.create({
    listContainer: {
        width: '100%',
        borderBottomWidth: 1,
        borderColor: 'red'
    },
});
